import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    txCidade:{
        position: "absolute",
        fontWeight: 'bold',
        top:10,
        fontSize: 20,
    }
})

export default styles